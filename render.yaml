services:
  - type: web
    name: kkn-absensi-qr-api
    env: node
    rootDir: server/
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    branch: main
    autoDeploy: true
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22
